__pycache__/
*.pyc
.venv/
.env
*.log

# Test coverage artifacts
.coverage
coverage.xml

# Apex script proposals written by sfdc_expert_enrich (staged for human review, not committed)
docs/oscal-salesforce-poc/apex-scripts/*.apex

.terraform/
*.tfstate
*.tfstate.*
crash.log
terraform.tfvars
*.tfvars
!.example.tfvars

# macOS Finder metadata
.DS_Store
**/.DS_Store

# Build artifacts
*.egg-info/

# Generated assessment outputs (real evidence committed intentionally, test artifacts excluded)
docs/oscal-salesforce-poc/generated/*/
!docs/oscal-salesforce-poc/generated/*.json

# Run artifacts — contain session state and local paths; never commit
docs/oscal-salesforce-poc/generated/**/loop_result.json
docs/oscal-salesforce-poc/generated/**/audit.jsonl

# report-gen deliverables output (generated artifacts, not tracked)
docs/oscal-salesforce-poc/deliverables/

# Runtime audit log and session state (never committed)
.saas-assurance/

# Claude Code session state
.claude/

# Internal session docs — contain local paths and personal info; stored in Obsidian vault
NEXT_SESSION.md
NEXT_SESSION_PROMPTS.md
